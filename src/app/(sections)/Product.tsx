import { Cards } from "@/components/Cards";

export function Product() {
  return (
    <section id="Produto" className="mt-[100px] flex w-full flex-col gap-[16px]">
      <Cards />
    </section>
  );
}
